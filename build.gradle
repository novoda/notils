buildscript {
    apply from: 'dependencies.gradle'
    repositories {
        jcenter()
    }
    dependencies {
        classpath libraries.build.bintrayRelease
    }
}

allprojects {
    version = "3.0.2"
}
