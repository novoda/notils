subprojects {
    buildscript {
        repositories {
            mavenCentral()
        }
    }

    repositories {
        mavenCentral()
    }
}

ext {
    javaCommonProject = project(':core')
    androidProject = project(':android')
    androidDebugClassesFolder = "intermediates/classes/live/debug"
    javaTestClassesFolder = "classes/test"

    monkeyMinVersion = 16
    minVersion = 19
    targetVersion = 20
    compileVersion = 20
    buildToolsVersion = "20.0.0"

    versionCode = 1
    versionName = "not defined"
}
