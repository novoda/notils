buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'com.novoda:bintray-release:0.2.4'
    }
}

apply from: 'dependencies.gradle'

allprojects {
    version = "3.0.0"
}
